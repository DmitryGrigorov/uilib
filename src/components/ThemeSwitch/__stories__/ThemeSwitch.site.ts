import { createComponentStand } from "../../../site";

export default createComponentStand({
  title: "Theme switch",
  id: "ThemeSwitch",
  group: "Neutrons",
  status: "release",
  subTitle: "Use it to switch between theme states.",
  description: "A switch requires two opposing values."
});
