import { createComponentStand } from "../../../site";

export default createComponentStand({
  title: "File uploader",
  id: "UploadDragFile",
  group: "Organisms",
  status: "release",
  subTitle: "Use UploadDragFile to upload files from anywhere on the site."
});
