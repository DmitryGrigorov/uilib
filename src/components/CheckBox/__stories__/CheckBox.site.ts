import { createComponentStand } from "../../../site";

export default createComponentStand({
  title: "Checkbox",
  id: "CheckBox",
  group: "Protons",
  status: "release",
  subTitle: "A building block for multiple-selection interfaces.",
  description: "Use it when people need to select multiple options from a list."
});
