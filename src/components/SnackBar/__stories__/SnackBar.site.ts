import { createComponentStand } from "../../../site";

export default createComponentStand({
  title: "SnackBar",
  id: "SnackBar",
  group: "Neutrons",
  status: "release",
  subTitle: "Low-priority notifications.",
  description:
    "Notifies the user about a completed action without blocking the interface."
});
