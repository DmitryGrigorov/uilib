import { createComponentStand } from "../../../site";

export default createComponentStand({
  title: "Progress indicator — Linear",
  id: "ProgressBar",
  group: "Neutrons",
  status: "release",
  subTitle:
    "The indicator shows that an element is loading or that a multi-step action is in progress"
});
