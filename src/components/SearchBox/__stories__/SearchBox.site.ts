import { createComponentStand } from "../../../site";

export default createComponentStand({
  title: "Search",
  id: "SearchBox",
  group: "Molecules",
  status: "release",
  subTitle:
    "SearchBox is a standardized component for searching across the system."
});
