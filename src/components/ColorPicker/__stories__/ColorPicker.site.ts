import { createComponentStand } from "../../../site";

export default createComponentStand({
  title: "Color picker",
  id: "ColorPicker",
  group: "Molecules",
  status: "release",
  subTitle: "A component for choosing colors.",
  description: "Use it to find and select a color."
});
