import { css } from "styled-components";

export const baseFontOnest = css`
  font-family: ${({ theme }) => theme.typography.fontFamily}, sans-serif;
`;

export const ariesOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightBlack};
    font-size: ${theme.typography.fontSize6xl};
    line-height: ${theme.typography.lineHeight6xl};
  `}
`;

export const taurusOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightBlack};
    font-size: ${theme.typography.fontSize5xl};
    line-height: ${theme.typography.lineHeight5xl};
  `}
`;

export const geminiOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightExtraBold};
    font-size: ${theme.typography.fontSize4xl};
    line-height: ${theme.typography.lineHeight4xl};
  `}
`;

export const cancerOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightExtraBold};
    font-size: ${theme.typography.fontSize3xl};
    line-height: ${theme.typography.lineHeight3xl};
  `}
`;

export const leoOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSize2xl};
    line-height: ${theme.typography.lineHeight2xl};
  `}
`;

export const virgoOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSizeXl};
    line-height: ${theme.typography.lineHeightXl};
  `}
`;

export const libraOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSizeL};
    line-height: ${theme.typography.lineHeightM};
  `}
`;

export const scorpiusOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSizeL};
    line-height: ${theme.typography.lineHeightL};
  `}
`;

export const capricornusOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSizeM};
    line-height: ${theme.typography.lineHeightM};
  `}
`;

export const saggitariusOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSizeM};
    line-height: ${theme.typography.lineHeightXs};
  `}
`;

export const aquillaOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightMedium};
    font-size: ${theme.typography.fontSizeL};
    line-height: ${theme.typography.lineHeightM};
  `}
`;

export const phoenixOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightMedium};
    font-size: ${theme.typography.fontSizeM};
    line-height: ${theme.typography.lineHeightM};
  `}
`;

export const cygnusOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightMedium};
    font-size: ${theme.typography.fontSizeS};
    line-height: ${theme.typography.lineHeightS};
  `}
`;

export const piscesOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightRegular};
    font-size: ${theme.typography.fontSizeL};
    line-height: ${theme.typography.lineHeightM};
  `}
`;

export const aquariusOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightRegular};
    font-size: ${theme.typography.fontSizeL};
    line-height: ${theme.typography.lineHeightL};
  `}
`;

export const corvusOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightRegular};
    font-size: ${theme.typography.fontSizeM};
    line-height: ${theme.typography.lineHeightM};
  `}
`;

export const lynxOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightRegular};
    font-size: ${theme.typography.fontSizeS};
    line-height: ${theme.typography.lineHeightS};
  `}
`;

export const cetusOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightRegular};
    font-size: ${theme.typography.fontSizeXs};
    line-height: ${theme.typography.lineHeight2xs};
  `}
`;

export const pavoOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightMedium};
    font-size: ${theme.typography.fontSizeXs};
    line-height: ${theme.typography.lineHeight2xs};
  `}
`;

export const muscaOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightRegular};
    font-size: ${theme.typography.fontSize2xs};
    line-height: ${theme.typography.lineHeight3xs};
  `}
`;

export const columbaOnest = css`
  font-style: normal;
  ${({ theme }) => css`
    font-family: ${theme.typography.fontFamily}, sans-serif;
    font-weight: ${theme.typography.fontWeightMedium};
    font-size: ${theme.typography.fontSize2xs};
    line-height: ${theme.typography.lineHeight3xs};
  `}
`;

export const ariesNeueMachina = css`
  font-style: normal;
  font-family: "Neue Machina", sans-serif;
  ${({ theme }) => css`
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSize6xl};
    line-height: ${theme.typography.lineHeight6xl};
  `}
`;

export const taurusNeueMachina = css`
  font-style: normal;
  font-family: "Neue Machina", sans-serif;
  ${({ theme }) => css`
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSize5xl};
    line-height: ${theme.typography.lineHeight5xl};
  `}
`;

export const geminiNeueMachina = css`
  font-style: normal;
  font-family: "Neue Machina", sans-serif;
  ${({ theme }) => css`
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSize4xl};
    line-height: ${theme.typography.lineHeight4xl};
  `}
`;

export const cancerNeueMachina = css`
  font-style: normal;
  font-family: "Neue Machina", sans-serif;
  ${({ theme }) => css`
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSize3xl};
    line-height: ${theme.typography.lineHeight3xl};
  `}
`;

export const leoNeueMachina = css`
  font-style: normal;
  font-family: "Neue Machina", sans-serif;
  ${({ theme }) => css`
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSize2xl};
    line-height: ${theme.typography.lineHeight2xl};
  `}
`;

export const virgoNeueMachina = css`
  font-style: normal;
  font-family: "Neue Machina", sans-serif;
  ${({ theme }) => css`
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSizeXl};
    line-height: ${theme.typography.lineHeightXl};
  `}
`;

export const libraNeueMachina = css`
  font-style: normal;
  font-family: "Neue Machina", sans-serif;
  ${({ theme }) => css`
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSizeL};
    line-height: ${theme.typography.lineHeightM};
  `}
`;

export const scorpiusNeueMachina = css`
  font-style: normal;
  font-family: "Neue Machina", sans-serif;
  ${({ theme }) => css`
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSizeL};
    line-height: ${theme.typography.lineHeightL};
  `}
`;

export const capricornusNeueMachina = css`
  font-style: normal;
  font-family: "Neue Machina", sans-serif;
  ${({ theme }) => css`
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSizeM};
    line-height: ${theme.typography.lineHeightM};
  `}
`;

export const saggitariusNeueMachina = css`
  font-style: normal;
  font-family: "Neue Machina", sans-serif;
  ${({ theme }) => css`
    font-weight: ${theme.typography.fontWeightBold};
    font-size: ${theme.typography.fontSizeM};
    line-height: ${theme.typography.lineHeightXs};
  `}
`;

export const baseFontNeueMachina = css`
  font-family: "Neue Machina", sans-serif;
`;
