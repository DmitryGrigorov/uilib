import { createComponentStand } from "../../../site";

export default createComponentStand({
  title: "Roll",
  id: "Roll",
  group: "Organisms",
  status: "release",
  subTitle: "An expandable interface control",
  description: "Opens over the interface and fills the entire layout."
});
