import { createComponentStand } from "../../../site";

export default createComponentStand({
  title: "Tag",
  id: "Tag",
  group: "Neutrons",
  status: "release",
  subTitle: "A filtering and sorting tool.",
  description:
    "A tag works both as a standalone component and as part of larger components."
});
