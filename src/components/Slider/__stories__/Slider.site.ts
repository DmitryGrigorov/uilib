import { createComponentStand } from "../../../site";

export default createComponentStand({
  title: "Slider",
  id: "Slider",
  group: "Molecules",
  status: "release",
  subTitle:
    "A slider is the primary control for displaying the current value and intervals within a range",
  description: "The slider tab indicates the value at its current position."
});
