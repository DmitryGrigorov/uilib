export interface IHeaderProps {
  /** Controls whether the UI Lib wordmark is rendered. */
  isLogo?: boolean;
  /** Uses the reduced header treatment intended for constrained sidebars. */
  compact?: boolean;
  className?: string;
}
